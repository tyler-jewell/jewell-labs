# Fix the bug

`sum_range.py` is supposed to return the sum of integers from `lo` to `hi` **inclusive**.

It currently fails unit expectations. Fix the implementation in place (edit `sum_range.py` only).

Do not modify anything outside this workspace.
